<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <!-- SignalR ships inside the ASP.NET Core shared framework since 3.0. The standalone
         Microsoft.AspNetCore.SignalR.Core package is 2.x-era and must not be referenced. -->
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

</Project>
